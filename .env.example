# Leochat 项目根目录 docker-compose.yaml 要求的环境变量
# 复制为 .env 后使用: docker compose up -d
# 系统环境变量 > .env > docker-compose.yaml

# Web 管理后台密码（不设置则自动生成随机密码）
WEB_PASSWORD=change-me

# AI Agent API Key（DeepSeek）
DEEPSEEK_API_KEY=""

# AI Agent 联网搜索（Tavily，可选，不设置则 agent 无搜索功能）
TAVILY_API_KEY=""
